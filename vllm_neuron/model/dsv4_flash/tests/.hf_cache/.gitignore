# The MQA-block layer-0 fixture is ~107 MB and would blow the repo up.
# Regenerated on first run of `test_mqa_1tensor.py` via HTTP-Range slice
# from HF snapshot `deepseek-ai/DeepSeek-V4-Flash-0731@7872f01b`, shard
# `model-00002-of-00048.safetensors`.  Small fixtures (like
# `dsv4_expert0_w2.safetensors`) stay checked-in because they fit under
# the per-file review budget.
dsv4_layer0_attn.safetensors
